(function () {

    var Promise = require("bluebird");
var fs = require("fs");

Promise.promisifyAll(fs);

    
    module.exports.readLayout = function(themename, layoutname){
        
    console.log('running');
    }
    module.exports.readTemplate = function(themename, category, name){
    var ppath = global.maindir + '\\themes\\' + themename + '\\templates\\' + category + '\\' + name + '.hbs';
        fs.readFile(ppath, (err, data) => {
  if (err) throw err;
  console.log(data.toString());
});
   

    }
    
}());
